//If no route matches to the current url;
const NotFound=() =>{
    return(
        <div className="notfound">
            <h1>This Component will be displayed when No Routes Path matches to the current URL Address!</h1>
        </div>
    )
}
export default NotFound;